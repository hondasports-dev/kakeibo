# PRレビュー

## 概要

- 変更概要:
- 比較基準: `origin/preview...HEAD`（または指定 base）
- ファイルごとの変更詳細:
- リスク（低／中／高）と理由:

## 正しさ

- 正常系:
- 異常系:
- 境界値:

## セキュリティ／プライバシー

- 機密情報（認証情報、トークン、APIキー等）やPIIの露出:
- 入力検証／注入（SQL、XSS等）の懸念:

## 保守性

- 責務分離／命名:
- 重複／抽象化:

## テスト

- 追加・更新されたテスト:
- 不足している観点（あれば）:

## 副作用・影響範囲

- 変更されていないが影響しうるファイル／機能:
- リスクの有無:

## 専門スキルレビュー結果

実行した専門スキルの結果を対象別にまとめる。対象外の場合は「対象外」と明記。

- `convex-performance-audit`（`convex/**` 変更時）:
  - 結果: PASS / 指摘あり / 対象外
  - 主な観点（DB 読み取り、OCC 競合、index、pagination）:
  - 指摘・改善案:
- `vercel-react-best-practices`（`src/**` React 変更時）:
  - 結果: PASS / 指摘あり / 対象外
  - 主な観点（再レンダリング、useEffect 依存、bundle）:
  - 指摘・改善案:
- `web-design-guidelines`（UI/UX 変更時）:
  - 結果: PASS / 指摘あり / 対象外
  - 主な観点（アクセシビリティ、コントラスト）:
  - 指摘・改善案:
- `prompt-injection-guard`（外部コンテンツ参照・実行がある場合）:
  - 結果: 問題なし / 隔離あり / 対象外
  - 検出された外部由来命令・隔離内容:
  - 許可状況:
- `service-ops-safety`（secret / 環境変数 / 外部サービス操作に関わる変更）:
  - 結果: 問題なし / 指摘あり / 対象外
  - 主な観点（機密情報流出、.env 操作、本番操作）:
  - 指摘・改善案:
- `virtual-company` Reviewer（認証 / Clerk 変更時）:
  - 結果: PASS / 指摘あり / 対象外
  - 主な観点（セキュリティ、認可）:
  - 指摘・改善案:

## テストケース判定レビュー

- 対象テストファイル:
- 正常系の判定:
- エッジケース:
- 準異常系:
- 異常系:
- アサーションの妥当性:
- 指摘（Must-fix / Nice-to-have）:

## 指摘

- Must-fix:
- Nice-to-have（各指摘に対象ファイルを明記）:
  - diff 内 → 修正済み:
  - diff 外のみ → フォローアップ Issue:

## レビュー判定

- [ ] PASS（Must-fix 0 件、diff 内 Nice-to-have はすべて修正済み、diff 外のみはフォローアップ Issue リンク済み）
- [ ] FAIL（未対応の Must-fix、または diff 内の未修正 Nice-to-have あり → 修正後に再レビュー）
