# 週1レシート入力Web家計簿 UI/UX設計

## 1. 目的

本ドキュメントは、MVP実装に向けたUI/UX方針を定義する。

本アプリのUIは、見た目の派手さよりも、週1回まとめて複数枚のレシートをリズムよく入力できることを最優先にする。

## 2. UX/UIコンセプト

コンセプトは「週末に溜まったレシートを、止まらず処理できる入力作業台」とする。

- 毎日入力する家計簿ではなく、週1回の片付け作業に最適化する
- 入力画面は、家計簿というよりレシート処理のワークベンチとして設計する
- 1枚入力するたびに、件数、合計、予算残額の変化がすぐ見えるようにする
- 主要操作は `保存して次へ` を中心に置く
- MUIを使うが、標準Material Design感は抑え、静かで軽い独自テーマにする
- Tailwind CSSは画面骨格、余白、レスポンシブなどのレイアウト用途に限定する

## 3. デザイン方針

### 3.1 見た目

- 家計簿向けの落ち着いた生活感を重視する
- 業務ツールに近い、読みやすく操作しやすいUIにする
- 装飾的なカードや大きなヒーロー表現は使わない
- 角丸は8px以下を基本にする
- 余白は広げすぎず、入力作業に必要な情報密度を保つ

### 3.2 カラー

- 主色は深めのグリーンまたはティールを候補にする
- 予算警告はアンバーを使う
- 予算超過や削除などの危険操作は控えめなレッドを使う
- カテゴリ色は区別しやすさを優先し、強すぎる彩度を避ける

### 3.3 タイポグラフィ

- 画面内の見出しは大きくしすぎない
- 金額、件数、予算残額は視認性を高める
- フォームラベルと補助文は短くする

## 4. ユーザーフロー

1. Googleアカウントでログインする
2. ダッシュボードで今週の支出状況を見る
3. `今週の入力を開始` または `入力を再開` を選ぶ
4. 週次入力画面でレシートを連続入力する
5. 入力しながら、件数、合計、予算残額、直近入力を確認する
6. 週次サマリーでカテゴリ別支出と前週比を見る
7. 振り返りメモを保存する
8. セッションを完了する

## 5. 画面構成

### 5.1 ダッシュボード `/`

目的は、今週の状況と入力再開導線をすぐ見つけられるようにすることである。

表示する情報:

- 今週の合計支出
- 週次予算
- 予算残額
- カテゴリ別支出
- 入力済みレシート件数
- 未完了セッション
- 前週比

主要操作:

- `今週の入力を開始`
- `入力を再開`
- `週次サマリーを見る`

注意:

- ダッシュボードを作り込みすぎない
- MVPでは、入力画面への導線を最も目立たせる

### 5.2 週次入力画面 `/weeks/current/input`

MVPで最も重要な画面とする。

PCでは、左に入力フォーム、右に今週サマリーと直近入力を配置する。

スマートフォンでは、入力フォームを最優先にし、サマリーは下部または折りたたみで表示する。

表示する情報:

- 入力フォーム
- 今週の入力済み件数
- 今週の合計支出
- 予算残額
- 直近入力したレシート一覧
- セッション完了導線

主要操作:

- `保存して次へ`
- `保存して完了`
- `直前の入力を取り消す`
- `直前を複製`

### 5.3 週次サマリー `/weeks/:weekStartDate`

目的は、入力後にその週の支出傾向を把握できるようにすることである。

表示する情報:

- 合計支出
- カテゴリ別支出
- 予算との差分
- 前週比
- 支出一覧

主要操作:

- 支出を修正する
- 振り返りへ進む

### 5.4 週次振り返り `/weeks/:weekStartDate/review`

目的は、支出傾向を見ながら次週に向けたメモを残せるようにすることである。

表示する情報:

- 週次サマリーの要点
- カテゴリ別支出
- 振り返りメモ入力欄

主要操作:

- `メモを保存`
- `セッションを完了`

### 5.5 カテゴリ設定 `/categories`

MVPでは深く作り込まず、入力体験を支える最低限の管理画面にする。

主要操作:

- カテゴリを追加する
- カテゴリ名を変更する
- カテゴリ色を変更する
- カテゴリを無効化する

## 6. レシート入力フロー

入力順は以下に固定する。

1. 日付
2. 店名
3. 金額
4. カテゴリ
5. メモ
6. 保存

### 6.1 日付

- 初期値は今日、または直前入力の日付にする
- 週内の日付を素早く選べるUIを用意する
- 保存後は前回値を引き継ぐ

### 6.2 店名

- MUI `Autocomplete` を使う
- 過去に入力した店名候補を上位表示する
- 保存後は店名欄にフォーカスを戻す

### 6.3 金額

- 入力値は日本円の整数として扱う
- 表示上は円表記を補助する
- スマートフォンでは数字キーボードを出しやすい入力属性にする
- 0円以下は保存できない

### 6.4 カテゴリ

- 店名からカテゴリ候補を推定する
- 推定できない場合は、直近カテゴリやよく使うカテゴリを表示する
- カテゴリは `Chip` または小さな `Button` でタップしやすくする
- 推定が外れても修正しやすいUIにする

### 6.5 メモ

- 任意入力とする
- 通常の入力リズムを邪魔しないよう、折りたたみまたは小さな欄にする
- 上限文字数を設ける

### 6.6 保存

主ボタンは `保存して次へ` とする。

保存後の挙動:

- 日付を引き継ぐ
- カテゴリを引き継ぐ、または店名から再推定する
- 店名と金額とメモをクリアする
- 店名欄へフォーカスする
- 直近入力リストへ即時反映する
- 今週の合計、件数、予算残額を更新する

補助ボタン:

- `保存して完了`
- `直前を複製`
- `直前の入力を取り消す`

### 6.7 画像から入力

画像入力は手入力を置き換える主導線ではなく、店名・日付・合計金額の候補を作る補助導線として扱う。

- `画像から入力` セクションはフォーム上部に置く
- 未選択時は、画像を選ぶとプレビューが出ることだけを短く示す
- 選択済み時は、プレビュー、ファイル名、削除、`読み取る` を表示する
- 読み取り時は、初回のみ外部API送信への同意ダイアログを表示する
- 同意済みの場合だけ画像を送信し、未同意または拒否時は送信しない
- ダイアログには、外部API送信、画像を長期保存しないこと、自動保存しないこと、手入力に戻れることを明記する
- 同意拒否時は、画像選択状態を解除して同じフォームで手入力できる状態に戻す
- 抽出成功時は候補だけをフォームへ反映し、保存は既存の `保存して次へ` に委ねる
- 抽出失敗時は Alert で `手入力でも保存できます。` を表示する

## 7. UI状態

### 7.1 空状態

- ダッシュボードでは `今週の入力を開始` を主導線にする
- 説明文は短くする
- 未入力でも不安を感じない表示にする

### 7.2 ローディング

- MUI `Skeleton` を使う
- フォーム全体をスピナーで塞がない
- 既に表示済みの入力内容はできるだけ保持する

### 7.3 保存中

- 保存ボタンのみ無効化する
- ボタン内に小さな `CircularProgress` を表示する
- 入力内容は消さない

### 7.4 保存成功

- MUI `Snackbar` で短く通知する
- 直近入力リストに即時追加して成功を視覚化する
- 通知は入力操作を邪魔しない位置に出す

### 7.5 入力エラー

- MUI `TextField` の `helperText` で項目ごとに表示する
- まとめエラーを上部に出しすぎない
- 金額、店名、カテゴリのエラーを優先して分かりやすくする

### 7.6 通信エラー

- MUI `Alert` と `再試行` ボタンを表示する
- オフラインキューはMVPでは実装しない
- 入力内容は消さない

### 7.7 セッション完了

- 完了後は `今週のサマリーを見る` を主ボタンにする
- 次に必要な操作を明確にする

## 8. MUIコンポーネント方針

MUIをUIコンポーネントの主軸にする。Tailwind CSSはMUIの代替ではなく、レイアウト補助として使う。

責務分担:

- MUI theme: 色、Typography、角丸、影、コンポーネント標準スタイル
- MUI `sx`: MUIコンポーネント単位の微調整、状態依存、theme参照が必要なスタイル
- MUI `styled`: 再利用する独自コンポーネントや複雑なスタイル
- Tailwind CSS: 画面全体のflex/grid、gap、padding、responsive、表示/非表示、外側ラッパー

`Button`、`TextField`、`Chip`、`Alert`、`Snackbar`、`Table` などのMUIコンポーネントの色、サイズ、角丸、状態表現はMUI themeを正とする。Tailwind CSSでMUIコンポーネント内部を深く上書きしない。

### 8.1 レイアウト

- `Container`
- `Stack`
- `Grid`
- `Box`

### 8.2 入力

- `TextField`
- `Autocomplete`
- `Select`
- `ToggleButtonGroup`
- `Button`

### 8.3 カテゴリ

- `Chip`
- 小さな `Button`

### 8.4 進捗と状態表示

- `LinearProgress`
- `Badge`
- `Skeleton`
- `CircularProgress`
- `Snackbar`
- `Alert`

### 8.5 一覧

- PCでは `Table` を使う
- スマートフォンでは縦積みのリスト表示にする
- スマートフォンで横スクロール前提の表は使わない

### 8.6 ナビゲーション

- PCではサイドナビまたはトップナビを使う
- スマートフォンでは下部ナビまたは上部の最小ナビを使う
- 入力画面ではナビゲーションを主張させすぎない

## 9. レスポンシブ方針

### 9.1 PC

- 左側に入力フォームを配置する
- 右側に今週サマリー、予算残額、直近入力を配置する
- キーボード操作を重視する
- 入力中に週次状況が常に見えるようにする

### 9.2 スマートフォン

- 1画面1タスクを基本にする
- `保存して次へ` を画面下部に固定する
- カテゴリはタップしやすいチップとして表示する
- 支出一覧はテーブルではなく縦積みリストにする
- サマリーは入力を妨げない位置に置く

## 10. 実装時の注意

- 入力フォームは再レンダリングでフォーカスが飛ばないようにする
- 保存後の初期値引き継ぎを仕様として固定する
- 金額は表示フォーマットと保存値を分ける
- カテゴリ推定は外れても邪魔にならないUIにする
- Tailwind CSSはレイアウト用途に限定し、MUIコンポーネントの見た目制御はMUI theme、`sx`、`styled` を優先する
- CSVエクスポートやオフライン対応は、入力セッションの導線から外す
- ダッシュボードよりも、週次入力画面を先に作り込む
- E2Eでは、5枚以上のレシートを連続入力できることを検証する

## 11. MVPで優先する実装順

1. MUIテーマ、Tailwind CSS、基本レイアウト
2. 週次入力画面
3. 保存後の `保存して次へ` フロー
4. 今週サマリーと直近入力の即時表示
5. ダッシュボード
6. 週次サマリー
7. 振り返りメモ
8. カテゴリ設定

## 12. 相談元

本方針は、`.agents/roles/optional-ux-ui-designer.md` の役割定義に基づいて、UX/UI Designerエージェントと相談した結果を整理したものである。
